version: "3.8"

services:
  db:
    image: "postgres:15-alpine"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=nexus_user
      - POSTGRES_PASSWORD=securenexuspassword
      - POSTGRES_DB=nexus_db
